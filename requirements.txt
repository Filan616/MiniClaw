# MiniClaw - Complete Dependencies
# Generated: 2026-06-05
# Install: pip install -r requirements.txt

# ============================================================
# Core Dependencies (Always Required)
# ============================================================

# Feishu (Lark) SDK for message handling
lark-oapi>=1.4

# OpenAI API client (supports OpenAI, DeepSeek, and compatible providers)
openai>=1.40

# Web framework for webhook server
fastapi>=0.110
uvicorn>=0.29

# HTTP client for provider API calls
httpx>=0.27

# Job scheduling (for cron tasks, heartbeat, etc.)
apscheduler>=3.10

# Database ORM
sqlalchemy>=2.0

# CLI framework
typer>=0.12

# Data validation and settings management
pydantic>=2.6
pydantic-settings>=2.2

# YAML config parsing
pyyaml>=6.0

# Better logging
loguru>=0.7


# ============================================================
# Optional: Browser Automation
# ============================================================
# Enables /browser skill for web scraping and interaction
# Two-step installation (Playwright uses separate browser binaries):
#   1. pip install playwright           # Install Python library (~10 MB)
#   2. playwright install chromium      # Download Chromium browser (~300 MB)
# Note: Browser binary is stored in ~/.cache/ms-playwright/, not in site-packages
# playwright>=1.44


# ============================================================
# Optional: RAG Vector Search (Phase 8 M4)
# ============================================================
# Heavy dependencies for semantic search with embeddings
# Required for: rag.embedding.enabled=true, vector_backend=chroma
# Install: pip install chromadb sentence-transformers
# chromadb>=0.4
# sentence-transformers>=2.2


# ============================================================
# Optional: RAG Code Anchors (Phase 8.3.5)
# ============================================================
# Enables incremental reindex with AST-based code anchors
# Required for: /context reindex <id> --incremental on code files
# Install: pip install tree-sitter tree-sitter-language-pack
# tree-sitter>=0.22
# tree-sitter-language-pack>=0.7


# ============================================================
# Development Dependencies
# ============================================================
# Testing framework
# pytest>=8.0
# pytest-asyncio>=0.23


# ============================================================
# Installation Instructions
# ============================================================
#
# Basic (core only):
#   pip install -r requirements.txt
#
# With all optional features:
#   pip install -r requirements.txt playwright chromadb sentence-transformers tree-sitter tree-sitter-language-pack
#   playwright install chromium
#
# Development environment:
#   pip install -e .
#   pip install -e '.[dev]'
#
# Production with vector search:
#   pip install -e '.[rag-vector]'
#
# Production with code anchors:
#   pip install -e '.[rag-code]'
#
# Full installation (all features):
#   pip install -e '.[browser,rag-vector,rag-code,dev]'
#
